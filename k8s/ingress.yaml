apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dress-dock-flow
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: dress-dock-flow
                port:
                  number: 80